diff --git a/docs/architecture/AECCT_HLS_Architecture_Guide_v12.1_zhTW.txt b/docs/architecture/AECCT_HLS_Architecture_Guide_v12.1_zhTW.txt
index b0a8c8a..5102734 100644
--- a/docs/architecture/AECCT_HLS_Architecture_Guide_v12.1_zhTW.txt
+++ b/docs/architecture/AECCT_HLS_Architecture_Guide_v12.1_zhTW.txt
@@ -92,6 +92,8 @@ Source baseline: AECCT_HLS_v12.1_logical_dataflow_zhTW.txt + AECCT_HLS_v12.1_phy
 
 3.1 建議的實體儲存類別
 - W_REGION：權重 / 參數 / metadata 的 persistent 區。
+- W_REGION 內的 ternary 權重以 packed payload 儲存，並帶 per-matrix inv_s_w 與對應 metadata。
+- matrix record 在本版是邏輯 section format，不等於手動固定 physical SRAM base offsets。
 - X_WORK：主 token × d 工作區，用於 preproc_x、layer outputs、endLN_out 等主工作矩陣；這些是邏輯值名稱，並非實體儲存名稱。
 - SCRATCH：attention K/V、FinalHead 與其他合法 scratch 子區的共用暫存區；SCR_K / SCR_V / FINAL_SCALAR_BUF 都屬此類。
 - IO_REGION：input / output / debug / meta staging 的合法區域。
@@ -107,6 +109,12 @@ Source baseline: AECCT_HLS_v12.1_logical_dataflow_zhTW.txt + AECCT_HLS_v12.1_phy
 - 單一 token / 單一 head / 單一 tile 的短生命週期狀態，是優先考慮 reg 的對象。
 - v1 的 quant caches 屬於「固定值快取」而非動態追蹤狀態：當前 layer / slot 所需的 s_x / inv_s_x / inv_s_w 可在 block 入口載入 local regs，但不在 run-time 回寫新的 scale table。
 
+3.3 Ternary packed weight 的線性化與 decode 建議
+- 權重矩陣的數學表示維持 W[out][in]；out 是列(Row)、in 是行(Colume)。
+- 「in contiguous」只表示實體線性化/存放順序，不改變 W[out][in] 的數學語意。
+- datapath 不建議在 inner-most MAC loop 直接做逐權重 bit extract；建議先以 tile 為單位 unpack/decode 到 local buffer，再消費 {-1, 0, +1}。
+- per-matrix inv_s_w 建議在 matrix section 入口與 payload metadata 一起讀取，再供該矩陣的 decode/運算路徑使用。
+
 ====================================================================
 4. 邏輯與實體的對照原則
 ====================================================================
@@ -363,10 +371,13 @@ A. W_REGION
   - embedding / positional information
   - BASE_SRC_MASK
   - Q/K/V/O/FFN/LN/FinalHead 權重與參數
+  - ternary packed weight payload（2-bit codebook）與 per-matrix inv_s_w
+  - matrix record/section metadata（包含 payload_words 與 last_word_valid_count）
   - quant 相關 metadata
 - 規則：
   - 不與 runtime scratch alias
   - LOAD_W 後在整次推論期間保持有效
+  - matrix record 為邏輯 section 格式；實體 base offsets 仍由 SramMap / WeightStreamOrder / generator 決定
 
 B. X_WORK（主 token × d 工作區）
 - 性質：主工作矩陣區。
@@ -486,6 +497,9 @@ G. DATA_OUT 路徑
   - 寫入 W_REGION
 - 覆蓋規則：
   - LOAD_W 完成後，W_REGION 不應被 runtime scratch 覆蓋
+ - 格式規則：
+   - ternary packed payload 需符合固定 codebook（00/01/11；10 為 illegal）
+   - last_word_valid_count 與尾端 padding 必須與 payload 一致
 
 1. Input ingest / Preproc 前段
 - 邏輯值：
diff --git a/docs/architecture/AECCT_SramMap_alias_freepoint_rules_v12.1_zhTW.txt b/docs/architecture/AECCT_SramMap_alias_freepoint_rules_v12.1_zhTW.txt
index d85fb6b..b773ba5 100644
--- a/docs/architecture/AECCT_SramMap_alias_freepoint_rules_v12.1_zhTW.txt
+++ b/docs/architecture/AECCT_SramMap_alias_freepoint_rules_v12.1_zhTW.txt
@@ -140,11 +140,14 @@ E. TOKEN_LOCAL / SMALL_SCRATCH
 - transformer weights
 - H_by_var / connectivity metadata
 - QUANT_PARAM metadata（如固定 s_x / inv_s_w）
+- ternary packed weight payload（每個 weight 2 bits，32-bit word 打包 16 個）
+- per-matrix inv_s_w 與 matrix record/section metadata（例如 payload_words、last_word_valid_count）
 
 規則：
 - LOAD_W 或等價 parameter 載入完成後，W_REGION 不應被 runtime scratch 覆蓋
 - W_REGION 不與 runtime scratch alias
 - W_REGION 的 physical layout 應配合 WeightStreamOrder / layout_kind / mask_kind 描述
+- matrix record 在本版屬邏輯 section format，不代表手動固定 physical base offset
 
 3.2 X_WORK
 ----------
@@ -327,6 +330,7 @@ free-point 例：
 ----------------------------------
 在 generator 完成前，本文件先凍結語意欄位，
 不手工維護 base offset / region size 數值。
+- ternary matrix record 亦同：先凍結邏輯欄位與 section 語意，實體位址仍由生成流程決定。
 
 6.2 建議保留的語意欄位
 ----------------------
@@ -345,7 +349,7 @@ SramMap / WeightStreamOrder 後續至少應能表達：
 - X_WORK：single main working buffer / phase-safe overwrite
 - SCR_K / SCR_V：layer-scoped scratch
 - FINAL_SCALAR：finalhead pass-bridge scratch
-- W_REGION：persistent parameter region
+- W_REGION：persistent parameter region（含 ternary packed payload + per-matrix inv_s_w + matrix record metadata）
 - IO_REGION：staging-only region
 
 ================================================================
diff --git a/docs/architecture/AECCT_module_interface_rules_v12.1_zhTW.txt b/docs/architecture/AECCT_module_interface_rules_v12.1_zhTW.txt
index b1978c2..7d982e6 100644
--- a/docs/architecture/AECCT_module_interface_rules_v12.1_zhTW.txt
+++ b/docs/architecture/AECCT_module_interface_rules_v12.1_zhTW.txt
@@ -193,6 +193,12 @@ phase_id 只描述正式 phase 邊界，不可濫用來隱藏額外 micro-state
 所有共享記憶體相關範圍資訊都由 Top 提供。
 block 不得自行推導出第二套與 Top 不一致的 base/len 規則。
 
+5.5 ternary packed payload contract
+-----------------------------------
+- ternary packed payload 的正式格式由 W_REGION + WeightStreamOrder 定義；block 不得自行假設另一套 ternary encoding。
+- LOAD_W / parameter ingest 需支援 packed payload + per-matrix inv_s_w + metadata（含 matrix record/section 欄位）。
+- matrix record 在本版是邏輯 section 格式，不等於手動固定 physical SRAM base offsets。
+
 ================================================================
 6. 各 block 的最低介面要求
 ================================================================
@@ -375,6 +381,7 @@ sub-block 不得直接驅動外部 ctrl_rsp。
 5. FinalHead 正式分為 Pass A / Pass B
 6. 正常輸出路徑以 direct data_out 為主
 7. 本版不把 block-internal request/rsp 當正式 contract
+8. ternary packed payload 格式唯一來源為 W_REGION + WeightStreamOrder
 
 ================================================================
 12. 待後續文件補齊的事項
diff --git a/docs/archive/spells/咒語v12.1_zhTW.txt b/docs/archive/spells/咒語v12.1_zhTW.txt
index f360993..df8a588 100644
--- a/docs/archive/spells/咒語v12.1_zhTW.txt
+++ b/docs/archive/spells/咒語v12.1_zhTW.txt
@@ -41,6 +41,17 @@ Version: 2026-03-10
 - 若 notebook / host / 轉檔腳本原始持有 s_w，應在離線整理或 streamer 準備階段先轉成 inv_s_w 再送入 unified PARAM stream。
 - WeightStreamOrder / PARAM metadata 必須能明確指出哪些欄位屬於固定 s_x，哪些欄位屬於 inv_s_w。
 
+(3A) ternary weight SRAM 存放硬釘子（v12.1 freeze）
+- ternary codebook 固定為：00=0、01=+1、11=-1、10=reserved/illegal。
+- 每個 ternary weight = 2 bits；每個 32-bit word = 16 個 ternary weights。
+- packing 固定 LSB-first：第 0 個權重放 word[1:0]，第 15 個權重放 word[31:30]。
+- 每個 ternary matrix 的最後一個 payload word 需帶 last_word_valid_count，合法範圍固定為 1..16；若最後一個 word 滿載，last_word_valid_count=16。
+- tail padding bits 一律為 0。
+- quant metadata 粒度固定為 per-matrix inv_s_w（s_w 僅保留數學語意，不是主流程必備儲存欄位）。
+- LOAD_W / parameter ingest / checker 若遇 ternary code=10，必須視為格式錯誤並回報 error，不得吞掉。
+- decode 策略固定為 tile-unpack 到 local buffer，再讓 datapath 消費 {-1,0,+1}；不建議在 inner-most MAC loop 做逐權重零碎 bit 抽取。
+- matrix record 視為邏輯 section format（matrix_id/rows/cols/num_weights/payload_words/last_word_valid_count/inv_s_w/layout_kind/payload），不代表手動固定最終 physical base offset。
+
 (4) 主流程保留 reserved 身分的項目
 - grouping / zone
 - SET_QCFG 或等價配置命令
@@ -1896,4 +1907,3 @@ TB 必須完成以下步驟（並能對每步做 timeout/錯誤碼檢查；每
   因此 WeightStreamOrder.h / weights_streamer.h 的 unified PARAM 已移除：LPE、LPE_PROJ_WEIGHT、LPE_PROJ_BIAS。
 - EXP_LEN_BIAS_WORDS / EXP_LEN_W_WORDS 已同步更新，TB/Top 以新常數為準。
 
-
diff --git a/docs/process/AECCT_v12_M0-M24_plan_zhTW.txt b/docs/process/AECCT_v12_M0-M24_plan_zhTW.txt
index a2963d1..b0a448f 100644
--- a/docs/process/AECCT_v12_M0-M24_plan_zhTW.txt
+++ b/docs/process/AECCT_v12_M0-M24_plan_zhTW.txt
@@ -129,6 +129,8 @@ M4 — SET_W_BASE + LOAD_W + WeightStreamOrder
 - LOAD_W
 - WeightStreamOrder.h
 - valid_bits / bitpack padding 檢查
+- ternary packed weight SRAM format 凍結（codebook/payload packing/last_word_valid_count）
+- per-matrix inv_s_w metadata 凍結
 - DESIGN 嚴禁 compile-time weights
 
 TB
@@ -136,10 +138,12 @@ TB
 - 對齊錯誤
 - 長度錯誤
 - padding bits 非零
+- ternary codebook 非法碼（10）必須回報格式錯誤
 - LOAD_W 完成並回到 IDLE 後 READ_MEM 比對
 
 DoD
 - 權重主路徑固定為「TB 串流進 SRAM」
+- LOAD_W / WeightStreamOrder 與凍結的 ternary SRAM 格式一致
 
 ==================================================
 M5 — in_fifo + IO_REGION + SET_OUTMODE + HALTED/DEBUG 基建
@@ -355,6 +359,7 @@ M16 — 離線生成器整合：SramMap / WeightStreamOrder / native quant metad
   - SramMap.h
   - WeightStreamOrder.h
 - WeightStreamOrder 能描述固定 s_x 與 inv_s_w sections
+- WeightStreamOrder / generator 能描述 ternary packed payload、last_word_valid_count 與 per-matrix inv_s_w metadata
 
 TB
 - 重新生成 headers 後，M14 regression 仍綠
@@ -387,6 +392,7 @@ M18 — Native quant 離線分析流程建立（固定 s_x / inv_s_w）
 - trace/weights/reference tensor 分析腳本
 - 固定 s_x 飽和率 / clipping 報表
 - inv_s_w / ternary weight 對照檢查
+- ternary packed codebook / packing / last_word_valid_count 一致性檢查
 - compare framework（先 tolerant，不凍結未來 grouping）
 
 TB
diff --git a/docs/spec/AECCT_HLS_Spec_v12.1_zhTW.txt b/docs/spec/AECCT_HLS_Spec_v12.1_zhTW.txt
index e1a542e..15ef65a 100644
--- a/docs/spec/AECCT_HLS_Spec_v12.1_zhTW.txt
+++ b/docs/spec/AECCT_HLS_Spec_v12.1_zhTW.txt
@@ -289,6 +289,7 @@ Source baseline: AECCT_HLS_Spec_v12_zhTW.txt + v12.1 pending/logical/physical 
 - 從 data_in 精確消費 EXP_LEN_PARAM_WORDS 個 u32 words。
 - 依序寫入 sram[param_base_word + i]。
 - BITPACK sections 必須通過 valid_bits / zero-padding 檢查。
+- ternary packed weight sections 必須通過 codebook 檢查：僅允許 00/01/11；若遇 10（illegal）必須視為格式錯誤並回報既有格式錯誤碼。
 - 成功後回 RSP_DONE(LOAD_W)。
 
 5.5 Legacy compatibility path
@@ -321,6 +322,17 @@ Source baseline: AECCT_HLS_Spec_v12_zhTW.txt + v12.1 pending/logical/physical 
 - 最後一個 word 中未使用的尾端 bits 必須為 0。
 - valid_bits 只對 BITPACK sections 有意義。
 
+6.3.1 Ternary weight SRAM encoding（v12.1 freeze）
+- ternary codebook 固定為：00=0、01=+1、11=-1、10=reserved/illegal。
+- 每個 ternary weight 佔 2 bits；每個 32-bit word 固定打包 16 個 ternary weights。
+- packing 採 LSB-first：第 i 個權重放在 word[(2*i)+1 : 2*i]；例如第 0 個權重對應 word[1:0]，第 15 個權重對應 word[31:30]。
+- 每個 ternary 權重矩陣的最後一個 payload word 都必須帶有 last_word_valid_count，且合法範圍固定為 1..16；若最後一個 word 滿載，last_word_valid_count=16。
+- tail padding bits 必須為 0；亦即最後一個 word 中超出 last_word_valid_count 的高位 pair 一律為 0。
+- 每個 ternary 權重矩陣在主流程中必須有 per-matrix inv_s_w；s_w 保留數學語意，但不是主流程必備儲存欄位。
+- matrix record 在本版定義為邏輯 section format，至少包含：matrix_id、rows、cols、num_weights、payload_words、last_word_valid_count、inv_s_w、layout_kind、payload。
+- 上述 matrix record 只定義邏輯格式，不代表在本版手動固定最終 physical SRAM base offsets；實體 offsets 仍由 SramMap / WeightStreamOrder / offline generator 決定。
+- 權重矩陣的數學表示維持 W[out][in]（out=Row, in=Colume）；「in contiguous」僅表示實體線性化/存放順序，不改變數學表示。
+
 6.4 H matrix 的正式數學定義
 - H 的唯一數學定義為 H[c][v]。
 - 列(Row) = check index c。
@@ -367,6 +379,7 @@ Source baseline: AECCT_HLS_Spec_v12_zhTW.txt + v12.1 pending/logical/physical 
 7.2 Logical regions
 - W_REGION
   - 存放 weights / params / bitpack
+  - 對 ternary 權重矩陣至少包含 packed payload、per-matrix inv_s_w 與 matrix record metadata
   - 受 SET_W_BASE + WeightStreamOrder.h 管控
 - IO_REGION
   - 選配的 input_y staging / output staging / debug dump / metadata tables
@@ -701,6 +714,7 @@ Source baseline: AECCT_HLS_Spec_v12_zhTW.txt + v12.1 pending/logical/physical 
 12.3 s_w / inv_s_w 規則（v1 正式要求）
 - 權重端必須保留 s_w 的量化語意。
 - 但 v1 正式規定：外部 parameter stream / WeightStreamOrder 只需保存 inv_s_w = 1 / s_w，不要求獨立保存 s_w。
+- 對每個 ternary 權重矩陣，主流程 metadata 的最小粒度為 per-matrix inv_s_w。
 - 若 host / notebook / 轉檔流程原始持有的是 s_w，則必須在離線生成或 streamer 準備階段先轉成 inv_s_w 再送入 unified PARAM stream。
 - quant parameter 的 SSOT / metadata 必須能區分 activation 端的 s_x 類參數與權重端的 inv_s_w 類參數。
 - Quantized linear kernel 內部若需要權重端比例還原，應使用 inv_s_w（或與其數學等價的固定形式），不得在 datapath 內臨時依賴未定義的 s_w 來源。
diff --git a/docs/milestones/P00-008_report.md b/docs/milestones/P00-008_report.md
new file mode 100644
index 0000000..893d028
--- /dev/null
+++ b/docs/milestones/P00-008_report.md
@@ -0,0 +1,91 @@
+# P00-008 Report — Ternary Weight SRAM Storage Spec Update (docs-only)
+
+## Goal / Scope
+- 任務目標：將 ternary weight 在 SRAM 的存放規格正式補入現有 docs，並收斂為可審核的凍結條文。
+- 任務範圍：僅 docs 更新；不修改 design/src/include/tb/scripts/gen。
+- 任務性質：documentation closure，不含 implementation closure。
+
+## Docs Baseline Used
+- 本輪只使用本次 repo zip 內現有 docs 作為唯一基線。
+- 未引用已刪除的舊外部規格檔。
+- 目標文件存在性檢查結果：
+- `docs/spec/AECCT_HLS_Spec_v12.1_zhTW.txt`：found
+- `docs/architecture/AECCT_HLS_Architecture_Guide_v12.1_zhTW.txt`：found
+- `docs/architecture/AECCT_SramMap_alias_freepoint_rules_v12.1_zhTW.txt`：found
+- `docs/architecture/AECCT_module_interface_rules_v12.1_zhTW.txt`：found
+- `docs/archive/spells/咒語v12.1_zhTW.txt`：found
+- `docs/process/AECCT_v12_M0-M24_plan_zhTW.txt`：found
+
+## Files Updated
+- `docs/spec/AECCT_HLS_Spec_v12.1_zhTW.txt`
+- `docs/architecture/AECCT_HLS_Architecture_Guide_v12.1_zhTW.txt`
+- `docs/architecture/AECCT_SramMap_alias_freepoint_rules_v12.1_zhTW.txt`
+- `docs/architecture/AECCT_module_interface_rules_v12.1_zhTW.txt`
+- `docs/archive/spells/咒語v12.1_zhTW.txt`
+- `docs/process/AECCT_v12_M0-M24_plan_zhTW.txt`
+
+## Per-File Changes
+- `AECCT_HLS_Spec_v12.1_zhTW.txt`
+- 新增 ternary weight SRAM encoding 條文（codebook、2-bit packing、32-bit=16 weights、LSB-first）。
+- 新增 `last_word_valid_count` 語意（1..16；滿載=16）與 tail padding=0。
+- 明確化 per-matrix `inv_s_w` 為主流程 metadata 粒度；`s_w` 保留數學語意。
+- 補 `LOAD_W/checker`：遇 `10`（illegal）必須回報格式錯誤。
+- 補 matrix record 欄位，並明確為 logical section format，不代表手動固定 physical SRAM base offsets。
+- 明確化 `W[out][in]`（數學）與 `in contiguous`（實體線性化）分離。
+
+- `AECCT_HLS_Architecture_Guide_v12.1_zhTW.txt`
+- 補 W_REGION 中 ternary packed payload + per-matrix `inv_s_w` + matrix record metadata 的使用語意。
+- 補 decode 策略：先 tile-unpack/decode 到 local buffer，再消費 `{-1,0,+1}`。
+- 補 per-matrix `inv_s_w` 讀取時機（matrix section 入口）。
+- 明確 `W[out][in]` 為數學表示，`in contiguous` 僅為實體存放順序。
+
+- `AECCT_SramMap_alias_freepoint_rules_v12.1_zhTW.txt`
+- 補 W_REGION 包含 ternary packed payload、per-matrix `inv_s_w`、matrix record/section metadata。
+- 強化 persistent/no-alias 語意：W_REGION 不得與 runtime scratch alias。
+- 補 matrix record 邏輯層定位：只凍結 logical section，不手工固定 physical base offset。
+
+- `AECCT_module_interface_rules_v12.1_zhTW.txt`
+- 補 interface contract：ternary packed payload 格式唯一來源為 `W_REGION + WeightStreamOrder`。
+- 補 block 限制：不得自訂另一套 ternary encoding。
+- 補 `LOAD_W/parameter ingest` 必須支援 packed payload + per-matrix `inv_s_w` + metadata。
+
+- `咒語v12.1_zhTW.txt`
+- 新增 v12.1 freeze 硬釘子：codebook、16/word、LSB-first、`last_word_valid_count`（1..16；滿載=16）、padding=0、per-matrix `inv_s_w`、illegal `10` 必報錯、tile-unpack decode。
+- 補 matrix record 為 logical section format，不等於手動固定 physical base offset。
+
+- `AECCT_v12_M0-M24_plan_zhTW.txt`
+- 在 M4/M16/M18 做最小補充，凍結 ternary packed SRAM format 與 per-matrix `inv_s_w` 對齊要求。
+- 補 `LOAD_W / WeightStreamOrder / generator` 需與凍結格式一致的里程碑條文。
+
+## Frozen Decisions Applied
+- codebook：`00=0`、`01=+1`、`11=-1`、`10=reserved/illegal`。
+- 2-bit packed ternary；32-bit word=16 weights；LSB-first。
+- `last_word_valid_count`：合法範圍 `1..16`；最後一個 word 滿載時為 `16`。
+- tail padding bits 一律為 `0`。
+- 主流程 metadata 粒度：per-matrix `inv_s_w`；`s_w` 保留數學語意。
+- `W[out][in]` 是數學表示；`in contiguous` 僅為實體線性化/存放順序。
+- 遇 `10` illegal code：`LOAD_W / parameter ingest / checker` 必須報格式錯誤。
+- matrix record 是 logical section format，不代表手動固定 physical base offsets。
+
+## Files Not Found in zip/docs
+- none
+
+## Verified / Not Verified
+- Verified
+- 6 份目標文件均存在，且已就地更新。
+- 關鍵規則已落入 spec/architecture/srammap/interface/spells/plan 六份文件。
+- 變更範圍維持 docs-only。
+
+- Not Verified
+- 本輪未做 code/generator/header/tb 實作驗證（依任務範圍排除）。
+- 本輪未新增或驗證 runtime 數值正確性測試（依任務範圍排除）。
+
+## Final Conclusion
+- P00-008 已完成 docs-only 的 ternary weight SRAM storage 規格更新與凍結收斂。
+- 本次結論僅代表文件收斂完成，不代表實作已完成。
+
+## README Auto Policy
+- This is a docs-only closure step and therefore does not trigger README Auto update.
+
+## Recommended Next Step
+- 以本輪凍結文件為單一來源，另立後續任務執行 `WeightStreamOrder / loader / checker` 的實作對齊與回歸驗證。
diff --git a/docs/milestones/P00-008_artifacts/file_manifest.txt b/docs/milestones/P00-008_artifacts/file_manifest.txt
new file mode 100644
index 0000000..3108b41
--- /dev/null
+++ b/docs/milestones/P00-008_artifacts/file_manifest.txt
@@ -0,0 +1,18 @@
+P00-008 file manifest
+
+existing file patched
+- docs/spec/AECCT_HLS_Spec_v12.1_zhTW.txt
+- docs/architecture/AECCT_HLS_Architecture_Guide_v12.1_zhTW.txt
+- docs/architecture/AECCT_SramMap_alias_freepoint_rules_v12.1_zhTW.txt
+- docs/architecture/AECCT_module_interface_rules_v12.1_zhTW.txt
+- docs/archive/spells/咒語v12.1_zhTW.txt
+- docs/process/AECCT_v12_M0-M24_plan_zhTW.txt
+
+new file added
+- docs/milestones/P00-008_report.md
+- docs/milestones/P00-008_artifacts/file_manifest.txt
+- docs/milestones/P00-008_artifacts/diff.patch
+- docs/milestones/P00-008_artifacts/verdict.txt
+
+generated file regenerated
+- none
diff --git a/docs/milestones/P00-008_artifacts/verdict.txt b/docs/milestones/P00-008_artifacts/verdict.txt
new file mode 100644
index 0000000..df23c2a
--- /dev/null
+++ b/docs/milestones/P00-008_artifacts/verdict.txt
@@ -0,0 +1 @@
+PASS: ternary weight SRAM storage documentation update completed. No implementation change is included in P00-008.
